#parameters, unpacking, variables
# we'lls ee another method so you can pass variables to the script.. script being the python code
from sys import argv
script, first, second, third = argv

print ("the script is called", script)
print (" the frst variable is called", first)
print (" the second variable is called", second)
print (" the third variable is called", third)

#importing is how we add features to the python script
#argv is the argument variable a very std name in pprogramming 
#argv holds the variables until theyre assigned to/ passed to the python script 
# the stufff we import to add functionality to our code are said to be the modules or libraries but lets stick with modules
# argv is given to the command line whereas the raw_input is given to the script while it is running
 print("--------------------------------------")
